<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Editar materia</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
<div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
    <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Editar materia</h1>

    {{#successMessage}}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4">
            <span class="block font-semibold mb-2">{{successMessage}}</span>
            <div class="flex gap-3 mt-2">
                <a href="/materia/list"
                   class="bg-gray-600 text-white px-3 py-1 rounded hover:bg-gray-700 text-sm">
                    Volver al listado
                </a>
            </div>
        </div>
    {{/successMessage}}

    {{#errorMessage}}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4">
            <span class="block font-semibold mb-2">{{errorMessage}}</span>
            <div class="flex gap-3 mt-2">
                <a href="/materia/list"
                   class="bg-gray-600 text-white px-3 py-1 rounded hover:bg-gray-700 text-sm">
                    Volver al listado
                </a>
            </div>
        </div>
    {{/errorMessage}}

    <form action="/materia/{{id}}/edit" method="post" class="space-y-4">
        <div>
            <label for="nombre" class="block text-gray-700 text-sm font-medium mb-1">Nombre:</label>
            <input type="text" id="nombre" name="nombre" value="{{nombre}}" required
                   class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <div>
            <label for="descripcion" class="block text-gray-700 text-sm font-medium mb-1">Descripción:</label>
            <textarea id="descripcion" name="descripcion" required rows="3"
                      class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">{{descripcion}}</textarea>
        </div>
        <div>
            <label for="codigo" class="block text-gray-700 text-sm font-medium mb-1">Código:</label>
            <input type="number" id="codigo" name="codigo" value="{{codigo}}" required
                   class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <button type="submit"
                class="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
            Guardar cambios
        </button>
        <div class="text-center mt-4">
            <a href="/materia/list" class="text-blue-600 hover:underline text-sm">Volver al listado</a>
        </div>
    </form>
</div>
</body>
</html>
